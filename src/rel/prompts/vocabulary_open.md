- Label each segment with a short verb-object phrase in lower snake_case,
  for example pick_cup, place_cup_in_bin, open_drawer.
- If a stretch contains no completed manipulation at all -- the robot fumbles,
  repeatedly fails to grasp, or is idle -- label it exactly
  `no_completed_subtask` rather than stretching a real event to cover it.
