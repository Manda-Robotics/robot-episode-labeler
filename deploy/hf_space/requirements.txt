# The `rel` package is copied into the Space by scripts/publish_hf_space.py, so
# only its runtime dependencies are listed here.
google-genai>=1.0.0
pillow>=10.4
pydantic>=2.9
numpy>=2.0
imageio-ffmpeg>=0.6.0
mcap>=1.4.0
gradio>=5
